package rest

import "github.com/gin-gonic/gin"

func (h *Handler) InitMessageRouter(r *gin.RouterGroup) {

	// TODO r.POST("/chats/:chat_id/messages", ...)
	// TODO r.GET("/chats/:chat_id/messages", ...)
}
